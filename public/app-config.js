window.HNS_CONFIG = {
    // Leave empty for the normal website version served by this Node server.
    // For the bundled iPhone app, set this to your Render backend URL, e.g.
    // 'https://your-service-name.onrender.com'
    serverUrl: 'https://phonehidenseek.onrender.com'
};
